language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chromeapp-coffee' ]; then cd .. && eval "mv $currentfolder generator-chromeapp-coffee" && cd generator-chromeapp-coffee; fi

